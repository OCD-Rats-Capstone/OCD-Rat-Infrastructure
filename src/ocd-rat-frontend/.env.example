# API URL for the backend
# For local development with Vite (npm run dev), use the full URL:
# VITE_API_URL=http://localhost:8000
#
# For production served by Nginx or behind a proxy:
# VITE_API_URL=/api
