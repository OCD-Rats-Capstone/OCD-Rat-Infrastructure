# Production override file
# Usage: docker compose -f docker-compose.yml -f docker-compose.prod.yml up -d --build

services:
  db:
    ports:
      - "5433:5432" # Prod db port

  backend:
    ports:
      - "8000:8000" # Prod backend port

  frontend:
    ports:
      - "80:80" # Prod frontend port
